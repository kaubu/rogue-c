# rogue-c

## Prerequisites

* C compiler (set in `justfile`)
* ncurses
* the Just command runner

## Building

Building then running (default):

```sh
just
```

Building:

```sh
just build
```

Running:

```sh
just run
```

List just commands:

```sh
just -l
# or
just --list
```
